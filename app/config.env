# Copy to .env at project root (gitignored) and fill in secrets.
# Settings class (app/config.py) reads from .env automatically.
INFLUX_ENABLED=true
INFLUX_DATABASE=iaq_predictions
INFLUX_HOST=87.106.102.14
INFLUX_PORT=8086

# API key for mutation endpoints. Empty = auth disabled (dev mode).
# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
API_KEY=